<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppUDP.Pages.UDP.BroadcastUdpDetailPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Receive.IP}"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Receive.Port}"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Receive.Response}"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Criar Botão" Command="{Binding CriarBotaoCommand}" />
            <Button Text="Close" BackgroundColor="Red" Command="{Binding CloseModalCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>